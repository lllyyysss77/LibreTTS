interface:
  display_name: "LibreTTS"
  short_description: "Repo-specific patterns and workflows for LibreTTS"
  default_prompt: "Use the LibreTTS repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true